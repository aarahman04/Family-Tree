{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm ci",
  "buildCommand": "if [ -d web ]; then cd web && npm run build && rm -rf ../dist && mv dist ../dist; else npm run build; fi",
  "outputDirectory": "dist",
  "framework": null
}
